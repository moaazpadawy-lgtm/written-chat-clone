PORT=3000
MONGO_URI=

FRONTEND_URL=http://localhost:5173
# Limit messages per socket: window in seconds and max messages per window
MSG_RATE_WINDOW_SEC=5
MSG_RATE_MAX=8

# Copy this file to .env and fill MONGO_URI with your MongoDB Atlas connection string
# If you don't provide MONGO_URI during development, the server will automatically start
# an ephemeral in-memory MongoDB (mongodb-memory-server) so messages persist while the
# server is running. For production use a real MongoDB Atlas URI.

# Optional: JWT secret for future auth features
JWT_SECRET=replace_this_with_a_long_random_secret